ModSentry — emoji pack two
modsentry.site

Only the new emoji. The five from the motion kit (pulse, loading, approve,
deny, alert) and every logo, banner and animation file are not repeated here.

All 128 px, transparent, brand ramp (#001020 -> #a8e6ff), well inside
Discord's 256 KB limit.

animated/
  scan       Light bar sweeps the shield — audit running     :scan:
  radar      Sweep and blip — watching the server            :radar:
  lock       Shackle snaps shut — channel locked             :lock:
  ban        Bar slams across — member banned                :ban:
  warn       Triangle pulses twice — formal warning          :warn:
  levelup    Chevrons march up — promotion, level gain       :rankup:
  typing     Chevrons bounce in turn — staff replying        :typing:
  online     Core pulses with rings — service healthy        :online:

static/
  mark       The plain shield — server icon, footer          :mark:
  check      Approved, allowed, done                         :yes:
  cross      Rejected, blocked                               :no:
  banned     Bar across dimmed chevrons                      :banned:
  warning    Caution notice                                  :warning:
  locked     Locked thread or channel                        :locked:
  ticket     Support ticket                                  :ticket:
  rank1      Trial staff — one chevron                       :rank1:
  rank2      Moderator — two chevrons                        :rank2:
  rank3      Admin — full stack                              :rank3:
  gavel      Moderation action, case closed                  :gavel:
  status     Live status indicator                           :status:
  filter     Swear filter, word list                         :filter:

The four new videos (feature reel, beta invite, loading loop, outro card)
are timeline pieces in the project — export MP4 from each page.
