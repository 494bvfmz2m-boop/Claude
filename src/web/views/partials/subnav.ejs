<% if (typeof sections !== 'undefined' && sections && sections.length > 1) { %>
<nav class="subnav">
  <% sections.forEach(s => { %>
    <a href="#<%= s.id %>"><%= s.label %></a>
  <% }) %>
</nav>
<% } %>
